// Configuration for Google Drive API
// Replace these values with your own Google Drive API credentials

const CONFIG = {
    // Get your API key from: https://console.cloud.google.com/apis/credentials
    API_KEY: 'YOUR_GOOGLE_DRIVE_API_KEY',
    
    // The Google Drive folder ID containing the PDFs
    // You can find this in the folder URL: https://drive.google.com/drive/folders/FOLDER_ID_HERE
    FOLDER_ID: 'YOUR_FOLDER_ID',
    
    // Make sure the folder is shared publicly (Anyone with the link can view)
    // or with the service account if using one
};
