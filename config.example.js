// Configuration for Google Drive API
// Set your API key once, then you can use any folder link in the application

const CONFIG = {
    // Get your API key from: https://console.cloud.google.com/apis/credentials
    // This only needs to be set once
    API_KEY: 'YOUR_GOOGLE_DRIVE_API_KEY',
    
    // Optional: Default folder link (can be changed in the UI anytime)
    // Example: 'https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrStUvWxYz'
    FOLDER_LINK: '',
};
